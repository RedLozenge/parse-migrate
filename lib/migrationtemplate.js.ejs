/* global exports */
'use strict';

exports.up = function(Parse, helpers) {
    // up must return a Parse.Promise() that it fulfills when the
    // migration is complete.
    return Parse.Promise.as();
};
